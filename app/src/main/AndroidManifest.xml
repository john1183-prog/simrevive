<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.john1183.simrevive">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Needed so notification action can launch activities from background -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:label="SIM Revive"
        android:theme="@style/Theme.SimRevive"
        android:supportsRtl="true">

        <!-- Main screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot listener — fires after every restart -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <!-- Standard boot -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Transsion/MediaTek fast-boot -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- HMD/others -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Handles the "Fix Now" notification button -->
        <receiver
            android:name=".QuickFixReceiver"
            android:exported="false" />

    </application>

</manifest>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.john1183.simrevive">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Bug 6 fix: removed SYSTEM_ALERT_WINDOW — declared but never used -->

    <application
        android:allowBackup="true"
        android:label="SIM Revive"
        android:theme="@style/Theme.SimRevive"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot listener -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Bug 5 fix: AlarmManager fires this 35s after boot (survives process death) -->
        <receiver
            android:name=".SimCheckReceiver"
            android:exported="false" />

        <!-- Handles "Fix Now" notification button -->
        <receiver
            android:name=".QuickFixReceiver"
            android:exported="false" />

    </application>

</manifest>
